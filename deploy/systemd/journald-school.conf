# Drop into /etc/systemd/journald.conf.d/school.conf and run
#   sudo systemctl restart systemd-journald
# to bound how much disk journald can use for the school-system bots' logs.

[Journal]
SystemMaxUse=500M
SystemMaxFileSize=50M
MaxRetentionSec=2week
